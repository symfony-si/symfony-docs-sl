days
~~~~

**tip**: ``array`` **privzeto**: 1 to 31

Seznam dnevov na voljo do dne tipa polja. Ta opcija je relevantna samo,
ko je opcija ``widget`` nastavljena na ``choice``::

    'days' => range(1,31)
